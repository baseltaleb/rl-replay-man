<ListView
    x:Class="RLReplayMan.BookmarkList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fview="clr-namespace:WpfTreeView;assembly=WpfTreeView"
    xmlns:local="clr-namespace:RLReplayMan"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{Binding Source={StaticResource mockViewModel}}"
    d:DesignHeight="450"
    d:DesignWidth="300"
    ItemsSource="{Binding Folders}"
    mc:Ignorable="d">

    <ListView.Resources>
        <local:MockViewModel x:Key="mockViewModel" />
    </ListView.Resources>

    <ListView.ItemTemplate>
        <DataTemplate>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image
                    Grid.Column="0"
                    Width="20"
                    Margin="3"
                    Source="{Binding ImageName, Converter={x:Static fview:HeaderToImageConverter.Instance}}" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{Binding Name}" />

            </Grid>
        </DataTemplate>
    </ListView.ItemTemplate>
</ListView>
